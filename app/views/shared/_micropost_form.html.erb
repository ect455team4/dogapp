<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, class: "field span12", :rows => "5"%>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-block btn-primary" %>
<% end %>

